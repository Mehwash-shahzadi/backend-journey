# Production Environment Configuration
# NEVER commit real secrets to git!
# Use proper secrets management (see README.md)

# ==================== Database ====================
# Use managed database services (RDS, Cloud SQL, etc.)
DATABASE_URL=postgresql://user:password@prod-db.example.com:5432/myapp_prod

# ==================== Redis ====================
# Use managed cache (ElastiCache, MemoryStore, etc.)
REDIS_URL=redis://prod-cache.example.com:6379

# ==================== API Keys ====================
# Store these in secrets manager, NOT in .env
GEMINI_API_KEY=
OPENAI_API_KEY=

# ==================== Security ====================
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# Store in secrets manager, NOT in .env
SECRET_KEY=

ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ==================== Application ====================
ENVIRONMENT=production

# NEVER set DEBUG=true in production!
DEBUG=false

APP_NAME=My FastAPI Backend
API_VERSION=1.0.0
