# Context window configuration
CONTEXT_WINDOW_MESSAGES = 20  # Number of recent messages to keep in context
MIN_MESSAGES_TO_KEEP = 10  # Minimum messages to preserve (user + assistant pairs)

# Title generation settings
DEFAULT_TITLE_WORD_LIMIT = 8